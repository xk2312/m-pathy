# Payment v1 – Test Harness (BS 14/1)

**Voraussetzung:** Lokaler Dev-Server läuft (`APP_BASE_URL`, Default `http://localhost:3000`).  
**ENV:** `.env.payment` oder `env.txt` im Repo-Root.

## Ausführen
```bash
chmod +x scripts/tests/*.sh
bash scripts/tests/run_all.sh
